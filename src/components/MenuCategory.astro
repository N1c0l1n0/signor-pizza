---
import MenuItem from './MenuItem.astro';
import type { MenuCategory } from '../types/content';

interface Props {
  category: MenuCategory;
}

const { category } = Astro.props;
---

<section id={category.id} class="category-section scroll-mt-36">
  <div class="mb-6 flex items-end justify-between border-b border-crema-forno pb-3">
    <div>
      <span class="text-xs font-bold uppercase tracking-widest text-pomodoro">Categoria</span>
      <h2 class="font-display text-2xl sm:text-3xl font-bold text-carbone">{category.name}</h2>
    </div>
    <span class="rounded-full bg-crema-forno px-3 py-1 text-xs font-semibold text-grigio-caldo">
      {category.items.length} pizze
    </span>
  </div>

  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    {category.items.map((item) => (
      <MenuItem item={item} categoryName={category.name} />
    ))}
  </div>
</section>
